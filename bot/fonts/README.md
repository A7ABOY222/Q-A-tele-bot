# Fonts

Place the following font files here for the best rank card experience:

- `Montserrat-Bold.ttf` — Used for usernames, level, and stats
- `Montserrat-Regular.ttf` — Used for secondary text

Download from https://fonts.google.com/specimen/Montserrat

If font files are absent, the bot will fall back to a default system font.
