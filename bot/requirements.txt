# Server Levels+ Discord Bot — Python dependencies

# Discord
discord.py>=2.3.2

# Database (SQLite async, default)
aiosqlite>=0.19.0

# Image generation (rank cards)
Pillow>=10.0.0
aiohttp>=3.9.0

# Environment variables
python-dotenv>=1.0.0

# Task scheduling (voice XP tracking, periodic tasks)
APScheduler>=3.10.4

# Fast async HTTP server for dashboard API
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# OAuth / JWT for dashboard authentication
python-jose[cryptography]>=3.3.0
httpx>=0.27.0

# Optional PostgreSQL support (uncomment if using PG)
# asyncpg>=0.29.0
# psycopg2-binary>=2.9.9

# Utilities
python-multipart>=0.0.9
